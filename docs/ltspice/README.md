# LTspice

The LTspice area contains circuit simulation setup. See
[`ltspice/README.md`](../../ltspice/README.md) for the current setup notes.

Document each new circuit model's assumptions, component values, simulation
cases, and resulting artifact so electrical decisions can be reviewed alongside
firmware and hardware changes.
