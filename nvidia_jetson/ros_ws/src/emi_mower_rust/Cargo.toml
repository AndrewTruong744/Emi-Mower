[package]
name = "emi_mower_rust"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Rust-side Emi Mower Zenoh helpers."

[dependencies]
# Keep the Rust binding on the same Zenoh wire-generation baseline as the
# Python client and the backend router image.
zenoh = "=1.9.0"
